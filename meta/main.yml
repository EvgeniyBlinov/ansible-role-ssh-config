---
dependencies:
  - role: "galaxy-ssh-config/defaults"
  - role: "galaxy-ssh-config/tools"
  - role: "galaxy-ssh-config/config"

galaxy_info:
  author: Evgeniy Blinov
  description: ssh config
  license: "license (MIT)"
  min_ansible_version: 2.4
  platforms:
    - name: Debian
      versions:
      - all
    - name: Ubuntu
      versions:
      - all
    - name: CentOS
      versions:
      - all
  galaxy_tags:
    - ssh_config
